name: 'BuildPulse Setup Docker Builder'
description: >-
  Use the local buildkit builder on BuildPulse runners, with a persistent layer cache. Drop-in for docker/setup-buildx-action.
author: BuildPulse
branding:
  icon: 'package'
  color: 'blue'
runs:
  using: 'node24'
  main: 'dist/index.js'
  post: 'dist/post.js'
  post-if: 'success()'
